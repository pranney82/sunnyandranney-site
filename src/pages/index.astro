---
import Base from '@/layouts/Base.astro';
import GoogleReviews from '@/components/GoogleReviews.astro';
import EmailCapture from '@/components/EmailCapture.astro';
import { getProducts, getCollections, shopifyImageUrl, shopifyImageSrcset } from '@/lib/shopify';

let products: any[] = [];
let collections: any[] = [];
let hasShopify = false;

// The 12 collections featured on the Shopify homepage, in order
const FEATURED_HANDLES = [
  'accent-chairs',
  'accent-end-tables',
  'bedroom-furniture',
  'coffee-tables',
  'consoles-sideboards-sofa-tables',
  'dining-tables',
  'sofas',
  'sectionals',
];

try {
  const result = await getProducts(8);
  products = result.products;
  hasShopify = products.length > 0;

  // Fetch collections and filter/sort to match Shopify homepage order
  const allCollections = await getCollections(10);
  const handleOrder = new Map(FEATURED_HANDLES.map((h, i) => [h, i]));
  collections = allCollections
    .filter((c: any) => handleOrder.has(c.handle))
    .sort((a: any, b: any) => (handleOrder.get(a.handle) ?? 99) - (handleOrder.get(b.handle) ?? 99));
} catch (e) {
  console.log('Shopify API not configured.');
}

// Split products by type for homepage sections
const onSaleProducts = products.filter(p => {
  const compare = p.compareAtPriceRange?.minVariantPrice;
  const price = p.priceRange.minVariantPrice;
  return compare && parseFloat(compare.amount) > parseFloat(price.amount);
});
const featuredProducts = products.slice(0, 8);

---

<Base lcpImage="https://imagedelivery.net/ROYFuPmfN2vPS6mt5sCkZQ/about/gracie/w=480,h=600,fit=cover,q=80,format=auto">
  <!-- ═══ HERO ═══ -->
  <section class="hero">
    <div class="container hero__inner">
      <div class="hero__text">
        <p class="hero__kicker animate-fade-up">Furniture with Heart</p>
        <h1 class="hero__title animate-fade-up delay-1">Shop with<br/>a Purpose</h1>
        <p class="hero__sub animate-fade-up delay-2">Curated furniture & home decor where every purchase transforms a child's room through <strong>Sunshine on a Ranney Day</strong>.</p>
        <div class="hero__actions animate-fade-up delay-3">
          <a href="/shop" class="btn btn--gold btn--hero" data-astro-prefetch="viewport">Shop &amp; Give Back</a>
        </div>
        <div class="hero__trust animate-fade-up delay-3">
          <span class="hero__trust-item">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
            Secure Checkout
          </span>
          <span class="hero__trust-sep"></span>
          <span class="hero__trust-item">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
            4.9 on Google
          </span>
          <span class="hero__trust-sep"></span>
          <span class="hero__trust-item">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>
            Roswell Pickup
          </span>
        </div>
      </div>
      <div class="hero__media animate-fade-up delay-2">
        <div class="hero__img-wrap">
          <img
            src="https://imagedelivery.net/ROYFuPmfN2vPS6mt5sCkZQ/about/gracie/w=480,h=600,fit=cover,q=80,format=auto"
            srcset="https://imagedelivery.net/ROYFuPmfN2vPS6mt5sCkZQ/about/gracie/w=320,h=400,fit=cover,q=75,format=auto 320w, https://imagedelivery.net/ROYFuPmfN2vPS6mt5sCkZQ/about/gracie/w=480,h=600,fit=cover,q=80,format=auto 480w, https://imagedelivery.net/ROYFuPmfN2vPS6mt5sCkZQ/about/gracie/w=640,h=800,fit=cover,q=80,format=auto 640w"
            sizes="(max-width: 767px) 240px, (max-width: 1023px) 340px, 380px"
            alt="Sunshine on a Ranney Day room makeover"
            width="480"
            height="600"
            fetchpriority="high"
            decoding="async"
          />
        </div>
        <div class="hero__accent" aria-hidden="true"></div>
      </div>
    </div>
    <div class="hero__ribbon">
      <div class="container hero__ribbon-inner">
        <span class="hero__ribbon-stat" data-count="269" data-suffix="+">0</span><span> rooms transformed</span>
        <span class="hero__ribbon-sep" aria-hidden="true"></span>
        <span class="hero__ribbon-stat" data-count="14" data-suffix="">0</span><span> years of giving</span>
        <span class="hero__ribbon-sep" aria-hidden="true"></span>
        <span><strong>100%</strong> of profits donated</span>
      </div>
    </div>
  </section>

  <!-- ═══ SHOP BY COLLECTION ═══ -->
  {collections.length > 0 && (
    <section class="collections-grid section">
      <div class="container">
        <div class="collections-grid__header reveal">
          <h2 class="section-title">Shop by Collection</h2>
          <a href="/shop" class="collections-grid__link" data-astro-prefetch>View all &rarr;</a>
        </div>
        <div class="collections-grid__items">
          {collections.map((col: any, idx: number) => {
            const colImage = col.image;
            const previewProduct = col.products?.edges?.[0]?.node;
            const imgUrl = colImage?.url || previewProduct?.images?.edges?.[0]?.node?.url;
            return (
              <a href={`/shop?collection=${col.handle}`} data-astro-prefetch class={`collection-tile reveal${idx > 0 ? ` delay-${Math.min(idx, 3)}` : ''}`}>
                <div class="collection-tile__image">
                  {imgUrl ? (
                    <img
                      src={shopifyImageUrl(imgUrl, 500, 500)}
                      srcset={shopifyImageSrcset(imgUrl, [250, 500])}
                      sizes="(max-width: 480px) 45vw, (max-width: 768px) 30vw, 200px"
                      alt={colImage?.altText || col.title}
                      width="500"
                      height="500"
                      loading={idx < 4 ? 'eager' : 'lazy'}
                      decoding="async"
                    />
                  ) : (
                    <div class="collection-tile__placeholder">
                      <span>{col.title.charAt(0)}</span>
                    </div>
                  )}
                  <div class="collection-tile__overlay">
                    <span>{col.title}</span>
                  </div>
                </div>
              </a>
            );
          })}
        </div>
      </div>
    </section>
  )}

  <!-- ═══ FEATURED / TRENDING ═══ -->
  {hasShopify && featuredProducts.length > 0 && (
    <section class="home-products section">
      <div class="container">
        <div class="home-products__header reveal">
          <h2 class="section-title">Trending Now</h2>
          <a href="/shop" class="home-products__see-all" data-astro-prefetch>See all &rarr;</a>
        </div>
        <div class="product-grid reveal" id="home-grid">
          {featuredProducts.map((product: any, idx: number) => {
            const image = product.images.edges[0]?.node;
            const image2 = product.images.edges[1]?.node;
            const price = product.priceRange.minVariantPrice;
            const compareAt = product.compareAtPriceRange?.minVariantPrice;
            const isOnSale = compareAt && parseFloat(compareAt.amount) > parseFloat(price.amount);
            const firstVariant = product.variants.edges[0]?.node;

            return (
              <div class="product-card" data-type={product.productType.toLowerCase()}>
                {isOnSale && <span class="product-card__badge">Sale</span>}
                {!isOnSale && !product.availableForSale && <span class="product-card__badge product-card__badge--sold">Sold Out</span>}
                <a href={`/shop/${product.handle}`} data-astro-prefetch class="product-card__image" aria-hidden="true" tabindex="-1">
                  {image ? (
                    <>
                      <img
                        src={shopifyImageUrl(image.url, 600)}
                        srcset={shopifyImageSrcset(image.url, [300, 600, 900])}
                        sizes="(max-width: 768px) 50vw, (max-width: 1024px) 33vw, 25vw"
                        alt=""
                        width={image.width}
                        height={image.height}
                        loading={idx < 4 ? 'eager' : 'lazy'}
                        fetchpriority={idx === 0 ? 'high' : undefined}
                        decoding={idx === 0 ? 'sync' : 'async'}
                        class="product-card__img-primary"
                      />
                      {image2 && (
                        <img
                          src={shopifyImageUrl(image2.url, 600)}
                          srcset={shopifyImageSrcset(image2.url, [300, 600, 900])}
                          sizes="(max-width: 768px) 50vw, (max-width: 1024px) 33vw, 25vw"
                          alt=""
                          width={image2.width}
                          height={image2.height}
                          loading="lazy"
                          decoding="async"
                          class="product-card__img-hover"
                        />
                      )}
                    </>
                  ) : (
                    <div class="product-card__image-ph">{product.productType || 'Product'}</div>
                  )}
                </a>
                <div class="product-card__info">
                  {product.productType && <span class="product-card__type">{product.productType}</span>}
                  <a href={`/shop/${product.handle}`} data-astro-prefetch class="product-card__title">{product.title}</a>
                  <div class="product-card__pricing">
                    <span class="product-card__price">${parseFloat(price.amount).toFixed(2)}</span>
                    {isOnSale && (
                      <span class="product-card__price--compare">${parseFloat(compareAt.amount).toFixed(2)}</span>
                    )}
                  </div>
                  {product.availableForSale && firstVariant && (
                    <button class="product-card__add-btn"
                      data-variant-id={firstVariant.id}
                      data-title={product.title}
                      data-price={parseFloat(price.amount)}
                      data-image={image?.url || ''}
                      data-handle={product.handle}
                      data-variant-title={firstVariant.title}
                    >
                      Add to Cart
                    </button>
                  )}
                </div>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  )}

  <!-- ═══ DESIGN SERVICES ═══ -->
  <section class="services section">
    <div class="container">
      <div class="services__header reveal">
        <h2 class="section-title">More Than a Furniture Store</h2>
      </div>
      <div class="services__grid reveal delay-1">
        <div class="services__item">
          <div class="services__icon-wrap">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="m12 19 7-7 3 3-7 7-3-3z"/><path d="m18 13-1.5-7.5L2 2l3.5 14.5L13 18l5-5z"/><path d="m2 2 7.586 7.586"/><circle cx="11" cy="11" r="2"/>
            </svg>
          </div>
          <h3 class="services__title">Design Assistance</h3>
          <p class="services__desc">Need help styling your space? Our in-house design team works with you to create rooms you'll love.</p>
        </div>
        <div class="services__item">
          <div class="services__icon-wrap">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76"/>
            </svg>
          </div>
          <h3 class="services__title">Custom Sourcing</h3>
          <p class="services__desc">Don't see what you're looking for? We can source furniture and decor beyond what's in our shop.</p>
        </div>
        <div class="services__item">
          <div class="services__icon-wrap">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M19 9V6a2 2 0 0 0-2-2H7a2 2 0 0 0-2 2v3"/><path d="M3 16a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-5a2 2 0 0 0-4 0v1.5a.5.5 0 0 1-.5.5h-9a.5.5 0 0 1-.5-.5V11a2 2 0 0 0-4 0z"/><path d="M5 18v2"/><path d="M19 18v2"/>
            </svg>
          </div>
          <h3 class="services__title">Custom Upholstery</h3>
          <p class="services__desc">Choose your own fabrics and finishes. We offer customizable upholstery on select pieces.</p>
        </div>
      </div>
      <div class="services__cta reveal delay-2">
        <a href="mailto:info@sunnyandranney.com" class="btn btn--outline">Get in Touch</a>
      </div>
    </div>
  </section>

  <!-- ═══ MISSION ═══ -->
  <section class="mission" id="mission">
    <div class="container mission__inner">
      <div class="mission__media reveal">
        <div class="mission__img-wrap">
          <img
            src="https://imagedelivery.net/ROYFuPmfN2vPS6mt5sCkZQ/about/founders-reunion-2026/w=600,q=80,format=auto"
            srcset="https://imagedelivery.net/ROYFuPmfN2vPS6mt5sCkZQ/about/founders-reunion-2026/w=600,q=80,format=auto 600w, https://imagedelivery.net/ROYFuPmfN2vPS6mt5sCkZQ/about/founders-reunion-2026/w=900,q=80,format=auto 900w, https://imagedelivery.net/ROYFuPmfN2vPS6mt5sCkZQ/about/founders-reunion-2026/w=1200,q=80,format=auto 1200w"
            sizes="(max-width: 767px) 90vw, (max-width: 1024px) 45vw, 540px"
            alt="Sunshine on a Ranney Day founders"
            width="600"
            height="400"
            loading="lazy"
            decoding="async"
          />
        </div>
      </div>
      <div class="mission__content reveal delay-1">
        <p class="mission__kicker">Our Why</p>
        <h2 class="mission__title">Every Purchase Changes a Child's World</h2>
        <p class="mission__body">
          When you shop at Sunny and Ranney, you're not just buying furniture.
          Through <strong>Sunshine on a Ranney Day</strong>, 100% of store profits
          fund bedroom and living space makeovers for children with special needs.
          269 rooms transformed. 14 years and counting.
        </p>
        <a href="/mission" class="btn btn--outline mission__cta" data-astro-prefetch>Read Our Full Story</a>
      </div>
    </div>
  </section>

  <!-- ═══ GOOGLE REVIEWS ═══ -->
  <GoogleReviews variant="full" />

  <!-- ═══ ON SALE (if any) ═══ -->
  {onSaleProducts.length > 0 && (
    <section class="home-products home-products--deals section">
      <div class="container">
        <div class="home-products__header reveal">
          <h2 class="section-title">On Sale</h2>
          <a href="/shop" class="home-products__see-all" data-astro-prefetch>Shop all deals &rarr;</a>
        </div>
        <div class="home-scroll reveal">
          {onSaleProducts.slice(0, 8).map((product: any) => {
            const image = product.images.edges[0]?.node;
            const price = product.priceRange.minVariantPrice;
            const compareAt = product.compareAtPriceRange?.minVariantPrice;
            return (
              <a href={`/shop/${product.handle}`} data-astro-prefetch class="home-scroll__card" aria-label={product.title}>
                <div class="home-scroll__image">
                  <span class="product-card__badge">Sale</span>
                  {image && (
                    <img
                      src={shopifyImageUrl(image.url, 400)}
                      srcset={shopifyImageSrcset(image.url, [220, 400])}
                      sizes="220px"
                      alt=""
                      width={image.width}
                      height={image.height}
                      loading="lazy"
                      decoding="async"
                    />
                  )}
                </div>
                <div class="home-scroll__info">
                  <span class="home-scroll__title">{product.title}</span>
                  <div class="home-scroll__pricing">
                    <span class="home-scroll__price">${parseFloat(price.amount).toFixed(2)}</span>
                    <span class="home-scroll__compare">${parseFloat(compareAt.amount).toFixed(2)}</span>
                  </div>
                </div>
              </a>
            );
          })}
        </div>
      </div>
    </section>
  )}

  <!-- ═══ VISIT STRIP ═══ -->
  <section class="visit-strip section">
    <div class="container visit-strip__inner">
      <div class="visit-strip__gallery">
        <div class="visit-strip__photo visit-strip__photo--exterior">
          <img
            src="https://imagedelivery.net/ROYFuPmfN2vPS6mt5sCkZQ/sunny-ranney-storefront/w=600,q=80,format=auto"
            alt="Sunny & Ranney storefront in Roswell, Georgia"
            width="600"
            height="450"
            loading="lazy"
            decoding="async"
          />
        </div>
        <div class="visit-strip__photo visit-strip__photo--interior">
          <img
            src="https://imagedelivery.net/ROYFuPmfN2vPS6mt5sCkZQ/sunny-ranney-interior/w=600,q=80,format=auto"
            alt="Inside the Sunny & Ranney showroom"
            width="600"
            height="450"
            loading="lazy"
            decoding="async"
          />
        </div>
      </div>
      <div class="visit-strip__details">
        <div class="visit-strip__info">
          <h3>Visit Our Roswell Showroom</h3>
          <p>109A Oak Street, Roswell, GA 30075</p>
          <p>Tue–Thu 10am–5pm &middot; Fri–Sat 10am–6pm</p>
          <p><a href="tel:6788885140">678.888.5140</a> &middot; <a href="mailto:info@sunnyandranney.com">info@sunnyandranney.com</a></p>
          <a href="https://maps.google.com/?q=109A+Oak+Street+Roswell+GA+30075" target="_blank" rel="noopener noreferrer" class="btn btn--outline">
            Get Directions
          </a>
        </div>
        <div class="visit-strip__map" id="map-container"
          data-src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3306.959536766344!2d-84.36101608805403!3d34.019249573057564!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x88f50d1cd2f8a7a7%3A0xfbc05f757f3bd1c2!2sSunny%20and%20Ranney!5e0!3m2!1sen!2sus!4v1773585890662!5m2!1sen!2sus">
          <div class="visit-strip__map-placeholder">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
            <span>Loading map…</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ RECENTLY VIEWED ═══ -->
  <section class="home-recent section" id="home-recent-section" style="display:none;">
    <div class="container">
      <div class="home-products__header">
        <h2 class="section-title">Pick Up Where You Left Off</h2>
        <a href="/shop" class="home-products__see-all" data-astro-prefetch>Continue shopping &rarr;</a>
      </div>
      <div class="home-recent__scroll" id="home-recent-scroll"></div>
    </div>
  </section>

  <!-- ═══ EMAIL CAPTURE ═══ -->
  <EmailCapture />
</Base>

<style>
  /* ─── Hero ─────────────────────────────── */
  .hero {
    background: linear-gradient(165deg, var(--color-cyan) 0%, #3d8aaa 60%, #357a98 100%);
    overflow: hidden;
  }

  .hero__inner {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-lg);
    padding-top: clamp(2rem, 5vh, 3.5rem);
    padding-bottom: 0;
    align-items: center;
  }

  .hero__text {
    padding-bottom: var(--space-lg);
  }

  .hero__kicker {
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-gold);
    margin: 0 0 var(--space-sm);
  }

  .hero__title {
    font-size: clamp(2.6rem, 8vw, 5.2rem);
    line-height: 0.92;
    letter-spacing: -0.025em;
    color: white;
    margin: 0 0 var(--space-md);
  }

  .hero__sub {
    color: rgba(255, 255, 255, 0.85);
    font-size: clamp(0.92rem, 1.1vw, 1.05rem);
    max-width: 38ch;
    margin: 0 0 var(--space-lg);
    line-height: 1.65;
  }

  .hero__sub strong {
    color: var(--color-gold);
    font-weight: 600;
  }

  .hero__actions {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    flex-wrap: wrap;
    margin-bottom: var(--space-md);
  }

  .hero__mission-link {
    font-size: 0.85rem;
    font-weight: 600;
    color: rgba(255,255,255,0.9);
    letter-spacing: 0.02em;
    transition: color 0.2s;
  }

  .hero__mission-link:hover {
    color: var(--color-gold);
  }

  /* Trust signals — inline in hero */
  .hero__trust {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    flex-wrap: wrap;
  }

  .hero__trust-item {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    font-size: 0.78rem;
    font-weight: 500;
    color: rgba(255,255,255,0.85);
    letter-spacing: 0.02em;
  }

  .hero__trust-item svg {
    opacity: 0.8;
  }

  .hero__trust-sep {
    width: 1px;
    height: 12px;
    background: rgba(255,255,255,0.2);
  }

  /* Hero image — contained, rounded, with accent */
  .hero__media {
    position: relative;
    display: flex;
    justify-content: center;
    padding-bottom: var(--space-md);
  }

  .hero__img-wrap {
    position: relative;
    z-index: 1;
    width: 85%;
    max-width: 300px;
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 24px 64px rgba(0,0,0,0.2), 0 8px 20px rgba(0,0,0,0.12);
  }

  .hero__img-wrap img {
    width: 100%;
    height: auto;
    display: block;
    aspect-ratio: 4/5;
    object-fit: cover;
    transition: transform 0.8s var(--ease-out);
  }

  .hero__img-wrap:hover img {
    transform: scale(1.03);
  }

  .hero__accent {
    position: absolute;
    bottom: -8px;
    right: -8px;
    width: 55%;
    height: 50%;
    background: var(--color-gold);
    border-radius: 20px;
    opacity: 0.2;
    z-index: 0;
  }

  /* Gold ribbon with inline stats */
  .hero__ribbon {
    background: var(--color-gold);
    padding: 1.1rem 0;
  }

  .hero__ribbon-inner {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.6rem;
    flex-wrap: wrap;
    font-size: 0.88rem;
    font-weight: 600;
    letter-spacing: 0.03em;
    color: var(--color-charcoal);
  }

  .hero__ribbon-stat {
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 700;
  }

  .hero__ribbon-sep {
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--color-charcoal);
    opacity: 0.35;
    margin: 0 0.5rem;
  }

  /* Mobile: tighter hero so products appear faster */
  @media (max-width: 767px) {
    .hero__inner {
      gap: var(--space-md);
      padding-top: var(--space-md);
    }

    .hero__text {
      padding-bottom: var(--space-sm);
    }

    .hero__title {
      margin-bottom: var(--space-sm);
    }

    .hero__sub {
      margin-bottom: var(--space-md);
    }

    .hero__actions {
      margin-bottom: var(--space-sm);
    }

    .hero__media {
      padding-bottom: var(--space-sm);
    }

    .hero__img-wrap {
      max-width: 240px;
    }
  }

  @media (min-width: 768px) {
    .hero__inner {
      grid-template-columns: 1.2fr 0.8fr;
      padding-top: clamp(2.5rem, 6vh, 4.5rem);
      gap: var(--space-xl);
    }

    .hero__text {
      padding-bottom: var(--space-xl);
    }

    .hero__media {
      justify-content: flex-end;
      padding-bottom: var(--space-lg);
    }

    .hero__img-wrap {
      width: 100%;
      max-width: 340px;
    }

    .hero__ribbon-inner {
      font-size: 0.95rem;
    }

    .hero__ribbon-stat {
      font-size: 1.4rem;
    }
  }

  @media (min-width: 1024px) {
    .hero__img-wrap {
      max-width: 380px;
    }
  }

  /* ─── Section Title ────────────────────── */
  .section-title {
    font-size: clamp(1.6rem, 3vw, 2.2rem);
    margin-bottom: var(--space-lg);
  }

  /* ─── Collections Grid ────────────────── */
  .collections-grid__header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
  }

  .collections-grid__header .section-title { margin-bottom: 0; }

  .collections-grid__link {
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--color-cyan);
    letter-spacing: 0.03em;
    transition: color 0.2s;
    flex-shrink: 0;
  }

  .collections-grid__link:hover { color: var(--color-charcoal); }

  .collections-grid__items {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.75rem;
    margin-top: var(--space-lg);
  }

  @media (min-width: 640px) {
    .collections-grid__items {
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
    }
  }

  @media (min-width: 1024px) {
    .collections-grid__items {
      grid-template-columns: repeat(4, 1fr);
      gap: 1rem;
    }
  }

  .collection-tile {
    display: block;
    text-decoration: none;
    color: var(--color-charcoal);
    transition: transform 0.4s var(--ease-out);
  }

  .collection-tile:hover {
    transform: translateY(-4px);
  }

  .collection-tile__image {
    position: relative;
    width: 100%;
    aspect-ratio: 1;
    overflow: hidden;
    border-radius: 12px;
    background: var(--color-cream-dark);
  }

  .collection-tile__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s var(--ease-out);
  }

  .collection-tile:hover .collection-tile__image img {
    transform: scale(1.06);
  }

  .collection-tile__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(0deg, rgba(0,0,0,0.55) 0%, rgba(0,0,0,0) 60%);
    display: flex;
    align-items: flex-end;
    padding: 0.75rem;
    transition: background 0.3s;
  }

  .collection-tile:hover .collection-tile__overlay {
    background: linear-gradient(0deg, rgba(0,0,0,0.65) 0%, rgba(0,0,0,0.05) 70%);
  }

  .collection-tile__overlay span {
    font-family: var(--font-display);
    font-size: clamp(0.82rem, 1.2vw, 0.95rem);
    font-weight: 600;
    color: white;
    line-height: 1.2;
    text-shadow: 0 1px 3px rgba(0,0,0,0.3);
  }

  .collection-tile__placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-charcoal);
    color: var(--color-gold);
    font-family: var(--font-display);
    font-size: 2.5rem;
    font-weight: 300;
  }

  /* ─── Home Products Header ─────────────── */
  .home-products { background: var(--color-warm-white); }

  .home-products__header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .home-products__header .section-title { margin-bottom: 0; }

  .home-products__see-all {
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--color-cyan);
    letter-spacing: 0.03em;
    transition: color 0.2s;
    flex-shrink: 0;
  }

  .home-products__see-all:hover { color: var(--color-charcoal); }

  .home-products .product-grid {
    margin-top: var(--space-lg);
  }

  /* ─── Image hover swap (homepage cards) ── */
  .product-card__img-primary,
  .product-card__img-hover {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: opacity 0.6s var(--ease-out), transform 0.8s var(--ease-out);
  }

  .product-card__img-hover { opacity: 0; }
  .product-card:hover .product-card__img-primary { opacity: 0; }
  .product-card:hover .product-card__img-hover { opacity: 1; transform: scale(1.03); }
  .product-card:hover .product-card__img-primary:only-child { opacity: 1; transform: scale(1.05); }

  @media (min-width: 768px) {
    .product-card__add-btn {
      opacity: 0;
      max-height: 0;
      padding: 0;
      margin: 0;
      overflow: hidden;
      transition: opacity 0.3s, max-height 0.3s, padding 0.3s, margin 0.3s;
    }
    .product-card:hover .product-card__add-btn {
      opacity: 1;
      max-height: 48px;
      padding: 0.55rem 1rem;
      margin-top: 0.5rem;
    }
  }

  /* ─── Horizontal Scroll (deals) ────────── */
  .home-scroll {
    display: flex;
    gap: 1rem;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
    padding-bottom: 0.5rem;
    margin-top: var(--space-lg);
    scrollbar-width: thin;
    scrollbar-color: var(--color-cream-dark) transparent;
  }

  .home-scroll__card {
    flex-shrink: 0;
    width: 220px;
    scroll-snap-align: start;
    background: var(--color-cream);
    border-radius: 8px;
    transition: transform 0.3s var(--ease-out), box-shadow 0.3s;
    overflow: hidden;
  }

  .home-scroll__card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 30px rgba(44,44,44,0.08);
  }

  .home-scroll__image {
    aspect-ratio: 4/5;
    overflow: hidden;
    background: var(--color-cream-dark);
    position: relative;
  }

  .home-scroll__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s var(--ease-out);
  }

  .home-scroll__card:hover .home-scroll__image img { transform: scale(1.05); }

  .home-scroll__info {
    padding: 0.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .home-scroll__title {
    font-family: var(--font-display);
    font-size: 0.9rem;
    font-weight: 500;
    line-height: 1.3;
    color: var(--color-charcoal);
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .home-scroll__pricing { display: flex; align-items: baseline; gap: 0.5rem; }
  .home-scroll__price { font-weight: 600; font-size: 0.9rem; color: var(--color-charcoal); }
  .home-scroll__compare { font-size: 0.8rem; color: var(--color-stone); text-decoration: line-through; }

  /* ─── Design Services ─────────────────── */
  .services {
    background: var(--color-cream);
  }

  .services__header {
    text-align: center;
  }

  .services__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-xl);
    margin-top: var(--space-xl);
  }

  @media (min-width: 640px) {
    .services__grid {
      grid-template-columns: repeat(3, 1fr);
      gap: var(--space-2xl);
    }
  }

  .services__item {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .services__icon-wrap {
    width: 68px;
    height: 68px;
    border-radius: 50%;
    background: var(--color-cyan);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: var(--space-md);
    transition: transform 0.4s var(--ease-out), box-shadow 0.4s;
  }

  .services__item:hover .services__icon-wrap {
    transform: translateY(-3px);
    box-shadow: 0 10px 28px rgba(74,163,188,0.25);
  }

  .services__title {
    font-family: var(--font-display);
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: var(--color-charcoal);
    letter-spacing: -0.01em;
  }

  .services__desc {
    font-size: 0.88rem;
    color: var(--color-stone);
    line-height: 1.65;
    max-width: 30ch;
    margin: 0 auto;
  }

  .services__cta {
    text-align: center;
    margin-top: var(--space-xl);
  }

  /* ─── Mission Section ──────────────────── */
  .mission {
    background: var(--color-charcoal);
    padding: clamp(3rem, 8vh, 5rem) 0;
  }

  .mission__inner {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-xl);
    align-items: center;
  }

  @media (max-width: 767px) {
    .mission {
      padding: var(--space-xl) 0;
    }
    .mission__inner {
      gap: var(--space-lg);
    }
  }

  @media (min-width: 768px) {
    .mission__inner {
      grid-template-columns: 0.9fr 1.1fr;
      gap: var(--space-2xl);
    }
  }

  .mission__media {
    order: -1;
  }

  .mission__img-wrap {
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 16px 48px rgba(0,0,0,0.3);
  }

  .mission__img-wrap img {
    width: 100%;
    height: auto;
    display: block;
    aspect-ratio: 3/2;
    object-fit: cover;
    transition: transform 0.8s var(--ease-out);
  }

  .mission__img-wrap:hover img {
    transform: scale(1.03);
  }

  .mission__kicker {
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-cyan-light);
    margin: 0 0 var(--space-sm);
  }

  .mission__title {
    color: white;
    font-size: clamp(1.6rem, 3.5vw, 2.5rem);
    line-height: 1.1;
    margin-bottom: var(--space-md);
  }

  .mission__body {
    color: rgba(255,255,255,0.75);
    font-size: 0.95rem;
    line-height: 1.7;
    max-width: 52ch;
    margin: 0 0 var(--space-lg);
  }

  .mission__body strong {
    color: var(--color-gold);
    font-weight: 600;
  }

  .mission__cta {
    border-color: rgba(255,255,255,0.3);
    color: white;
  }

  .mission__cta:hover {
    background: var(--color-gold);
    border-color: var(--color-gold);
    color: var(--color-charcoal);
  }

  /* ─── Visit Strip ──────────────────────── */
  .visit-strip {
    background: var(--color-gold);
  }

  .visit-strip__inner {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-lg);
  }

  @media (min-width: 768px) {
    .visit-strip__inner {
      grid-template-columns: 1fr 1fr;
      align-items: start;
    }
  }

  .visit-strip__gallery {
    position: relative;
    /* reserve space for the overlapping interior shot */
    padding-bottom: 2rem;
    padding-right: 2rem;
  }

  .visit-strip__photo img {
    width: 100%;
    height: auto;
    border-radius: 8px;
    display: block;
    object-fit: cover;
  }

  .visit-strip__photo--exterior {
    position: relative;
    z-index: 1;
    box-shadow: 0 4px 20px rgba(0,0,0,0.15);
    border-radius: 8px;
    overflow: hidden;
  }

  .visit-strip__photo--interior {
    position: absolute;
    bottom: 0;
    right: 0;
    width: 45%;
    z-index: 2;
    box-shadow: 0 4px 20px rgba(0,0,0,0.2);
    border-radius: 8px;
    overflow: hidden;
    border: 3px solid var(--color-gold);
  }

  @media (max-width: 767px) {
    .visit-strip__gallery {
      padding-bottom: 1.5rem;
      padding-right: 1.5rem;
    }
    .visit-strip__photo--interior {
      width: 50%;
    }
  }

  .visit-strip__details {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .visit-strip__info h3 {
    font-size: 1.3rem;
    margin-bottom: 0.3rem;
    color: var(--color-charcoal);
  }

  .visit-strip__info p {
    font-size: 0.88rem;
    color: var(--color-charcoal);
    margin: 0.15rem 0;
    max-width: none;
  }

  .visit-strip__info a { color: var(--color-charcoal); font-weight: 600; }

  .visit-strip__info .btn--outline {
    margin-top: var(--space-sm);
    display: inline-block;
  }

  .visit-strip .btn--outline {
    border-color: var(--color-charcoal);
    color: var(--color-charcoal);
    flex-shrink: 0;
  }

  .visit-strip .btn--outline:hover {
    background: var(--color-charcoal);
    color: var(--color-gold);
  }

  .visit-strip__map {
    width: 100%;
    border-radius: 8px;
    overflow: hidden;
  }

  .visit-strip__map iframe {
    display: block;
    width: 100%;
  }

  .visit-strip__map-placeholder {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    height: 250px;
    background: #e8d9a0;
    border-radius: 8px;
    color: var(--color-charcoal);
    font-size: 0.85rem;
  }

  /* ─── Product card add to cart (homepage) ── */
  .product-card__add-btn {
    margin-top: 0.75rem;
    width: 100%;
    padding: 0.6rem 1rem;
    background: var(--color-charcoal);
    color: var(--color-cream);
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    border: none;
    cursor: pointer;
    transition: background-color 0.3s, color 0.3s;
  }

  .product-card__add-btn:hover {
    background: var(--color-gold);
    color: var(--color-charcoal);
  }

  @media (min-width: 768px) {
    .home-products .product-card {
      border-radius: 4px;
    }
    .home-products .product-card__info {
      padding: 1rem 0.25rem 0.25rem;
    }
    .home-products .product-card__type {
      display: none;
    }
  }

  /* ─── Recently Viewed (homepage) ────────── */
  .home-recent { background: var(--color-cream); }

  .home-recent__scroll {
    display: flex;
    gap: 1rem;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
    padding-bottom: 0.5rem;
    margin-top: var(--space-lg);
    scrollbar-width: thin;
    scrollbar-color: var(--color-cream-dark) transparent;
  }

  .home-recent__card {
    flex-shrink: 0;
    width: 200px;
    scroll-snap-align: start;
    background: var(--color-warm-white);
    border-radius: 8px;
    transition: transform 0.3s var(--ease-out), box-shadow 0.3s;
    overflow: hidden;
  }

  .home-recent__card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 30px rgba(44,44,44,0.08);
  }

  .home-recent__image {
    aspect-ratio: 4/5;
    overflow: hidden;
    background: var(--color-cream-dark);
  }

  .home-recent__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s var(--ease-out);
  }

  .home-recent__card:hover .home-recent__image img { transform: scale(1.05); }

  .home-recent__info {
    padding: 0.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }

  .home-recent__title {
    font-family: var(--font-display);
    font-size: 0.88rem;
    font-weight: 500;
    line-height: 1.3;
    color: var(--color-charcoal);
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .home-recent__price {
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--color-charcoal);
  }
</style>

<script>
  // ─── Animated counters ───────────────────
  const counterEls = document.querySelectorAll('[data-count]');
  if (counterEls.length > 0) {
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    const animateCounter = (el: Element) => {
      const target = parseInt(el.getAttribute('data-count') || '0', 10);
      const suffix = el.getAttribute('data-suffix') || '';

      // Respect reduced motion — show final value immediately
      if (prefersReducedMotion) {
        el.textContent = target.toString() + suffix;
        return;
      }

      const duration = 1600;
      const start = performance.now();
      const step = (now: number) => {
        const progress = Math.min((now - start) / duration, 1);
        const eased = 1 - Math.pow(1 - progress, 3);
        el.textContent = Math.round(eased * target).toString() + suffix;
        if (progress < 1) requestAnimationFrame(step);
      };
      requestAnimationFrame(step);
    };
    const counterObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          animateCounter(entry.target);
          counterObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.5 });
    counterEls.forEach(el => counterObserver.observe(el));
  }

  // ─── Recently viewed on homepage ───────────
  const recentSection = document.getElementById('home-recent-section');
  const recentScroll = document.getElementById('home-recent-scroll');
  if (recentSection && recentScroll) {
    try {
      const items = JSON.parse(localStorage.getItem('sr_recent') || '[]');
      if (items.length > 0) {
        recentSection.style.display = '';
        const frag = document.createDocumentFragment();
        items.slice(0, 8).forEach((item: any) => {
          const card = document.createElement('a');
          card.href = `/shop/${encodeURIComponent(item.handle)}`;
          card.className = 'home-recent__card';

          const imgWrap = document.createElement('div');
          imgWrap.className = 'home-recent__image';
          if (item.image) {
            const img = document.createElement('img');
            img.src = item.image.includes('cdn.shopify.com') ? `${item.image}?width=400&crop=center` : item.image;
            img.alt = item.title || '';
            img.loading = 'lazy';
            imgWrap.appendChild(img);
          }
          card.appendChild(imgWrap);

          const info = document.createElement('div');
          info.className = 'home-recent__info';
          const title = document.createElement('span');
          title.className = 'home-recent__title';
          title.textContent = item.title;
          info.appendChild(title);
          if (item.price) {
            const price = document.createElement('span');
            price.className = 'home-recent__price';
            price.textContent = `$${parseFloat(item.price).toFixed(2)}`;
            info.appendChild(price);
          }
          card.appendChild(info);
          frag.appendChild(card);
        });
        recentScroll.replaceChildren(frag);
      }
    } catch (e) {
      console.warn('Recently viewed failed to load:', e);
    }
  }

  // Add to cart from homepage product cards
  const homeGrid = document.getElementById('home-grid');

  homeGrid?.addEventListener('click', (e) => {
    const btn = (e.target as Element)?.closest('.product-card__add-btn');
    if (!btn) return;
    e.preventDefault();
    e.stopPropagation();

    const { variantId, title, price, image, handle, variantTitle } = (btn as HTMLElement).dataset;
    if (!variantId || !title || !price) return;

    window.addToCart?.({
      variantId,
      title,
      price: parseFloat(price),
      image: image || '',
      handle: handle || '',
      variantTitle: variantTitle || '',
    });

    const orig = btn.textContent;
    btn.textContent = 'Added';
    (btn as HTMLButtonElement).disabled = true;
    setTimeout(() => {
      btn.textContent = orig;
      (btn as HTMLButtonElement).disabled = false;
    }, 1500);
  });

  // ─── Lazy-load Google Maps iframe on scroll ───
  const mapContainer = document.getElementById('map-container');
  if (mapContainer) {
    const mapSrc = mapContainer.dataset.src;
    const loadMap = () => {
      if (!mapSrc || mapContainer.querySelector('iframe')) return;
      const iframe = document.createElement('iframe');
      iframe.src = mapSrc;
      iframe.width = '100%';
      iframe.height = '250';
      iframe.style.cssText = 'border:0;border-radius:8px;display:block;';
      iframe.allowFullscreen = true;
      iframe.referrerPolicy = 'no-referrer-when-downgrade';
      iframe.title = 'Sunny & Ranney showroom location';
      mapContainer.replaceChildren(iframe);
    };
    const idle = (cb: () => void) => 'requestIdleCallback' in window
      ? requestIdleCallback(cb)
      : setTimeout(cb, 1);
    const observer = new IntersectionObserver((entries) => {
      if (entries[0].isIntersecting) {
        observer.disconnect();
        idle(loadMap);
      }
    }, { rootMargin: '200px' });
    observer.observe(mapContainer);
  }
</script>
